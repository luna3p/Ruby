class File_pattern

  pattern=Regexp.new(ARGV[0])

  filename=ARGV[1]


  file=open(filename)do |f|
    
    while text=f.gets do
      if pattern =~ text
        print text
      end
    end
  end

end